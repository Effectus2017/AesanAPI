import { Route } from '@angular/router';
import { BudgetComponent } from './budget.component';

export const budgetRoutes: Route[] = [
  { path: '', component: BudgetComponent } // Ruta para el componente Budget
];