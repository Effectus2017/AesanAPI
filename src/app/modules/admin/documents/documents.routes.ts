import { Route } from '@angular/router';
import { DocumentsComponent } from './documents.component';

export const documentsRoutes: Route[] = [
  { path: '', component: DocumentsComponent } // Ruta para el componente Documents
];